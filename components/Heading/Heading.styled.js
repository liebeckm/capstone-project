import styled from "styled-components";

const Heading = styled.h1`
  text-align: center;
  color: var(--white);
  background-color: var(--greyishred);
  margin-top: 0rem;
  margin-right: -0.5rem;
  padding: 1rem;
  letter-spacing: 0.2rem;
  font-weight: 150;
  font-size: 40px;
  overflow-y: overlay;
`;
export default Heading;
